# Aventraxo website
Files are separated for easier future edits.

- `index.html` — page structure and content
- `css/style.css` — design and responsive/mobile styling
- `js/main.js` — mobile menu interaction
- `assets/logo.svg` — Aventraxo brand mark

Open `index.html` locally to preview. Upload the whole folder contents to the GitHub Pages repository.
